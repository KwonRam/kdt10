<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Form 전송</title>
</head>
<body>
    <h1>Form</h1>

    <h2>Get 요청</h2>

    <form action="/login" method="get">
        <label for="id">ID</label>
        <input type="text" name="id" placeholder="ID" id="id">
        <label for="pw">
            PW
            <input type="password" name="pw" placeholder="PW" id="pw">
        </label>
        <!-- form 태그 안에 있는 button 태그는 type: submit(기본)-->
        <button>제출</button>
    </form>

    <h2>Post 요청</h2>
<!--pattern 속성: 폼 요서에 값 입력시 그 입력된 값에 대해 검증-->
    <form action="/login" method="post">
        <!--메서드가 다르면 url이 달라도 상관없음-->
        <label for="id">ID</label>
        <input type="text" name="id" placeholder="ID" id="id2" pattern="^([a-zA-Z0-9가-힣]){4,12}$" title="아이디는 대소문자, 숫자, 한글로 구성된 4~12자리" required><!-- 시작은 ^ 끝은 $-->
        <!-- required 속성: 폼 요소에 값이 반드시 들어가야하는지 여부를 결정-->
        <label for="pw">
            PW
            <input type="password" name="pw" placeholder="PW" id="pw2">
        </label>
        <!-- form 태그 안에 있는 button 태그는 type: submit(기본)-->
        <button>제출</button>
    </form>
    <!-- JS로 폼 유효성 검사하기 -->
    <h2>JS 유효성 검사</h2>
    <form action="/js-form-check" method="post" name="valiForm" onsubmit="return checkForm(this)" target="_blank">
        <input type="text" name="id" minlength="1" placeholder="ID" title="최소1글자">
        <span>우편물 수령장소</span>
        <input type="checkbox" name="post" id="자택">자택
        <input type="checkbox" name="post" id="회사">회사
        <button>제출</button>
    </form>
    <script>
        /*function checkForm() {
            const form= document.forms['valiForm']
            console.log(form);
        }*/
        function checkForm(form) {
            //const form= document.forms['valiForm']
            console.log(form);

            if(form.id.value == '') {
                form.id.focus();
                alert('아이디를 입력해주세요.');
                return false;
            }
        }
    </script>
</body>
</html>